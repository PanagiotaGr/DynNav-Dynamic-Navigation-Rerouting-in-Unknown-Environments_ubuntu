from setuptools import setup

package_name = 'dynnav_turtlebot3'

setup(
    name=package_name,
    version='0.1.0',
    packages=[],
    data_files=[],
    install_requires=['setuptools'],
    zip_safe=True,
    description='DynNav TurtleBot3 Gazebo integration',
)
